server:
  port: 9800
spring:
  application:
    name: ly-registry
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:9800/eureka
    fetch-registry: false
    register-with-eureka: false